# DEPUTYTASK — NESSUN TASK ATTIVO

> **Convenzione**: questo file viene **sovrascritto** ad ogni nuovo task delegato al deputy.
> Quando non ci sono task attivi, il file contiene solo questa intestazione.
>
> Per assegnare un nuovo task: sovrascrivere questo file con obiettivo, file coinvolti,
> fasi di esecuzione, criteri di chiusura (TEST OK / FIX NON APPLICABILI).
>
> Lo storico dei task completati si trova in `docs/archive/agent-tasks-2026/` e in `docs/GUIDA_CONSOLIDATA.md`.
