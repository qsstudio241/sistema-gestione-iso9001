# Dependencies
node_modules/
app/node_modules/

# Build output
app/dist/
app/build/

# Python (script locali, es. backend/scripts/pdf_to_json/)
__pycache__/
*.pyc

# Environment variables
.env
.env.local
.env.development
.env.production

# Credenziali SQL locali / VPS clone (template versionato: backend/config/database.json.example)
backend/config/database.json

# Nome sessione PuTTY per deploy script (solo etichetta sessione, mai password)
backend/config/.putty-session.local
# Deploy SSH non interattivo (copiare da .ssh-deploy.local.ps1.example — mai committare)
backend/config/.ssh-deploy.local.ps1

# Netlify CLI token (copiare da .netlify.local.ps1.example — mai committare)
backend/config/.netlify.local.ps1

# Evidenze diagnostico UNI Store (screenshot/report locali, possono contenere dati account)
backend/scripts/uni-store-artifacts/

# Output di prova pdf_to_json (.md/.json possono contenere estratti estesi di testo normativo
# protetto da copyright — mai committare, solo scratch locale per revisione manuale)
out/

# IDE
.vscode/
.idea/
.vs/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.cache/
.parcel-cache/
.vite/

# Playwright MCP cloud agent (snapshot/console log temporanei delle sessioni)
.playwright-mcp/

# Testing
coverage/

# Temporary files
*.tmp
*.temp

# Lock files (opzionale - alcuni team li committano)
# package-lock.json
# yarn.lock

# Local Netlify folder
.netlify

# Database backups (contengono dati sensibili)
database/backups/

# File Google Drive
*.gdoc
*.gsheet
*.gslides
*.gform
.cursor/mcp.json
.cursor/mcp.env
.cursorrrules

backend/scripts/patch-test-db-config-vps.js

# Cursor agent artefatti temporanei
.cursor/_*.cjs
.cursor/_*.mjs
.cursor/rbac-smoke-*.txt
.cursor/*-result.json
.cursor/_pr-body-*.md

